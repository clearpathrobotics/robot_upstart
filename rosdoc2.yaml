## Default configuration, generated by rosdoc2.

## This 'attic section' self-documents this file's type and version.
type: 'rosdoc2 config'
version: 1

---

settings: {}
builders:
    ## The sphinx_sourcedir points rosdoc2 at this package's own Sphinx project in doc/,
    ## replacing rosdoc2's default generated output.
    - sphinx: {
        sphinx_sourcedir: 'doc',
      }
